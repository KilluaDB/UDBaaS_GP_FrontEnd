import 'package:flutter/material.dart';

class SchemaVisualizer extends StatelessWidget {
  const SchemaVisualizer({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Done');
  }
}