import 'package:flutter/material.dart';

class DrawerTable extends StatelessWidget {
  const DrawerTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}