# UDBaaS_GP_FrontEnd
UDBaaS_GP_FrontEnd — A Flutter Web interface for the Unified Database as a Service (UDBaaS) platform. This project provides a responsive and interactive dashboard where developers can manage databases, create schemas, execute queries, and monitor performance metrics — all through an intuitive web-based UI built entirely with Flutter.
